<div class="posts list-group">
{{#each posts as |post| }}
<div class="card">
    <a href="/post/{{post.id}}">
        <h1 class="card-header">
            {{post.title}}
        </h1>
    </a>
    <p class="card-body">
        {{post.body}}
    </p>
</div>
{{/each}}
</div>